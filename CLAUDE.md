# Repo notes

- Git remote `origin` is `https://github.com/perinbarajan-star/ops-dashboard.git`.
- The old remote (`perinbarajan-star/Naturals-at-Home-dashboard`) was deleted; do not
  re-add it or push there.
- Default branch is `main`. Push work to `origin main` on this repo unless told
  otherwise for a specific task.
